name: barg_challenge
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  equatable: ^2.0.3
  get: ^4.6.1
  freezed: ^1.1.1
  freezed_annotation: ^1.1.0
  flutter_hooks: ^0.18.2
  flutter_localizations:
    sdk: flutter
  flutter_screenutil: ^5.0.2
  get_it: ^7.2.0
  google_fonts: ^2.2.0
  base_platform:
    path: packages/base_platform

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.7
  lint: ^1.8.1

flutter:
  uses-material-design: true

  assets:
    - assets/
